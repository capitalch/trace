
import 'package:flutter/material.dart';

class Jakar extends StatelessWidget {
  const Jakar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  const Scaffold(
      body: Center(child: Text('Jakar'),),
    )
    ;
  }
}
