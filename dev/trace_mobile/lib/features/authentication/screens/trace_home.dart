import 'package:flutter/material.dart';

class TraceHome extends StatelessWidget {
  const TraceHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      title: const Text('Trace authenticate'),
      ),
      body: const Text('Home'),
    );
  }
}
