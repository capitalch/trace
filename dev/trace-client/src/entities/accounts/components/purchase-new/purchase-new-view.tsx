import { Box, Button, Tab, Tabs, Typography, useTheme } from '../../../../imports/gui-imports'

function PurchaseNewView(){
    const theme = useTheme()
    return (<Box sx={{ mt: theme.spacing(2) }}>Purchase view</Box>)
}
export {PurchaseNewView}